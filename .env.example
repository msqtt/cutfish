# Optional: host the matching @ffmpeg/core 0.12.6 UMD files yourself.
# The directory must contain ffmpeg-core.js and ffmpeg-core.wasm with CORS enabled.
NEXT_PUBLIC_FFMPEG_CORE_BASE_URL=https://unpkg.com/@ffmpeg/core@0.12.6/dist/umd
