// Empty module stub for browser bundler fallback.
// Used to satisfy `fs` and `path` imports that exist in onnxruntime
// (bundled by @diffusionstudio/vits-web) but are never reached at runtime.
module.exports = {};
